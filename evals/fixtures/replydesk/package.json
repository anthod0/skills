{
  "name": "replydesk",
  "version": "0.1.0",
  "private": true,
  "description": "Turn customer messages and known facts into a reply worth sending.",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node build",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "svelte-kit sync && vitest run",
    "prepare": "svelte-kit sync"
  },
  "dependencies": {
    "@sveltejs/kit": "^2",
    "openai": "^6",
    "svelte": "^5"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5",
    "@sveltejs/vite-plugin-svelte": "^6",
    "@types/node": "^22",
    "svelte-check": "^4",
    "typescript": "~5.9",
    "vite": "^7",
    "vitest": "^3"
  },
  "engines": {
    "node": ">=22.12"
  }
}
