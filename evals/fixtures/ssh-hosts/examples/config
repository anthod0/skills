# The NAS stays on the home network.
Host nas
  HostName 192.168.1.20
  User sam
  IdentityFile "~/.ssh/home_ed25519"

# Development box, reachable through the VPN.
Host devbox
  HostName 10.8.0.14
  User deploy
  Port 2222
  ForwardAgent no

Host *
  ServerAliveInterval 30
  ServerAliveCountMax 3
