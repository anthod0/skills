FROM node:22-bookworm-slim
RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates git ripgrep \
    && rm -rf /var/lib/apt/lists/*
RUN npm install --global --ignore-scripts @earendil-works/pi-coding-agent@0.85.1
COPY pi-entry.mjs /harness/pi-entry.mjs
ENV PI_CODING_AGENT_DIR=/run/pi-agent PI_OFFLINE=1 PI_TELEMETRY=0
USER node
WORKDIR /workspace
ENTRYPOINT ["/usr/bin/timeout", "--signal=KILL", "180s", "node", "/harness/pi-entry.mjs"]
