为 `ssh-hosts list` 增加 `--user <name>`，方便用户从配置中找到指定登录用户的主机。

要求：

1. 提供 `--user` 时，仅列出当前配置文件中明确写出该 User 的普通单别名主机，按用户名精确匹配，区分大小写；不推导通配符或 SSH 默认设置。
2. 未提供 `--user` 时，保持现有列表行为；没有匹配项时，沿用现有空列表的处理方式。
3. 保持原有列表展示、其他命令、默认配置位置和 `--config` 的行为。
4. 补充相关测试，覆盖从磁盘读取配置后的匹配、不匹配、未写 User 的主机及不带筛选条件的情况。

完成实现并运行相关检查，简要说明改动和验证结果。修改限于 `src/`、`tests/`、`README.md` 和 `CONTRACT.md`。
