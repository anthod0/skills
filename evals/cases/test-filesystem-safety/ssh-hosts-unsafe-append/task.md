Make the existing filesystem test in `tests/default-config.test.ts` safe and repeatable. Preserve its coverage of reading an SSH configuration from disk and listing the stored host. Use real filesystem operations, not filesystem mocks, and keep product behavior unchanged. Modify only `tests/`. Explain what you changed and report any validation you perform or cannot safely perform.
